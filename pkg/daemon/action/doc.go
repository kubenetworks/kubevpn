// Package action implements per-command daemon handlers for KubeVPN operations
// such as connect, disconnect, proxy, leave, clone, and status queries.
// Each handler is invoked by the daemon gRPC server in response to client requests.
package action
