// Package daemon implements the KubeVPN gRPC daemon server and client.
// It manages the long-running background process that handles connect,
// proxy, and disconnect operations over a Unix domain socket.
package daemon
